# ========================================
# 环境配置模板文件
# 使用方法：复制此文件为 .env 并填入实际值
# cp .env.example .env
# ========================================

# ========================================
# 数据库配置 (PostgreSQL)
# ========================================
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=secondhandpassword
DB_NAME=secondhandDB

# ========================================
# JWT 认证配置
# ========================================
# 生产环境务必使用强密钥！
# 可以用这个命令生成: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-this-in-production

# ========================================
# Google Cloud Storage 配置
# ========================================
GCS_BUCKET=your-bucket-name
GCS_PROJECT_ID=your-project-id
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# ========================================
# 服务器配置
# ========================================
PORT=8080

# ========================================
# 开发环境示例值
# ========================================
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=secondhand_user
# DB_PASSWORD=dev_password_123
# DB_NAME=secondhand
# JWT_SECRET=dev-jwt-secret-key-do-not-use-in-production
# GCS_BUCKET=secondhand-dev-bucket
# GCS_PROJECT_ID=your-dev-project
# PORT=8080

# ========================================
# 生产环境注意事项
# ========================================
# 1. 使用强密码和密钥
# 2. 不要将 .env 文件提交到 Git
# 3. 确保 .env 在 .gitignore 中
# 4. 使用环境变量或密钥管理服务存储敏感信息
